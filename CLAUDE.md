# EathWrit のプロジェクトルール

## 申し送り（最優先）

**セッションの最初に [docs/HANDOFF.md](docs/HANDOFF.md) を読んでください。** 前のセッションが残した未解決事項がそこにあります。

**セッションの終わりに、次の 4 つのいずれかに当てはまるものがあれば HANDOFF.md に追記してください。**

- コードを読んだだけでは分からない判断や、その理由
- 気づいたが直さなかったこと（範囲外・判断待ち・確認手段がないなど）
- 意図的に見送った選択肢と、見送った理由
- 確認できていないこと（実機が必要、外部サービスが必要、など）

逆に、**やった作業の記録は書かないでください。** それは git log とコミットメッセージの役目です。HANDOFF.md が作業ログになると、肝心の未解決事項が埋もれます。

書き方は HANDOFF.md 冒頭のテンプレートに従ってください。片付いた項目は「解決済み」に移します。

大きな設計判断で経緯を残したいものは `docs/superpowers/specs/YYYY-MM-DD-<topic>.md` に別ファイルとして書き、HANDOFF.md からリンクします。

## docs をコードと一緒に更新する

README.md / README_ja.md / llms.txt は、機能・行数・デフォルト値をコードから引いて書いています。**UI かデフォルト値を変えたら、同じセッションで docs も直してください。** 後回しにすると食い違いに気づけません。

- 3 ファイルは互いに整合している必要があります。片方だけ直さないこと
- 行数・ファイル数・デフォルト値は `wc -l` やソースで**実測**してから書く。記憶で書かない
- UI レイアウトを変えたら `docs/images/` のスクリーンショットを撮り直す（6 枚：ライト/ダークの 24pt・60pt、外観メニュー、コピー＆消去のトースト）
- 英語版と日本語版で内容を揃える。日本語版はですます調

docs の書き方そのものは `readme-llm-optimize` スキルに従っています。

## コミットに混ぜないもの

- `DEVELOPMENT_TEAM` は空文字のままにします。実機ビルドのたびに Xcode が書き込むので、コミット前に戻してください
- `build/` は `.gitignore` 済みです

## このリポジトリの性質

コントリビューションは募集していません。フォークして各自の道具に作り変えてもらう前提です。機能を足すときは「使い捨てバッファ 1 つ」という前提を壊さないか確認してください。メモアプリには育てません。
